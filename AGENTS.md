# Agent Instructions

Bitte keine Binärdateien anfassen, erstellen oder verschieben. Die einzigen Binärartefakte im letzten Commit sind das Favicon (`frontend/src/app/favicon.ico`) sowie die beiden Schriftdateien (`frontend/src/app/fonts/GeistMonoVF.woff` und `frontend/src/app/fonts/GeistVF.woff`). Diese Dateien dürfen nicht verändert oder verschoben werden, sondern sollen lediglich unverändert referenziert werden.
